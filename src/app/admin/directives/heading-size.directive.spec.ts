import { HeadingSizeDirective } from './heading-size.directive';

describe('HeadingSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingSizeDirective();
    expect(directive).toBeTruthy();
  });
});
